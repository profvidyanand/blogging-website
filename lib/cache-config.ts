/** Default ISR TTL for public blog pages (15 hours). */
export const PUBLIC_REVALIDATE_SECONDS = 54000;

/** Sitemap uses the same TTL as public pages. */
export const SITEMAP_REVALIDATE_SECONDS = 54000;
